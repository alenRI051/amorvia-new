# Example environment variables for Amorvia

# Required: random secret used for hashing IPs in /api/track
TRACK_SALT=changeme

# Optional: events per 5min window (default 60)
TRACK_RATE_LIMIT=60

# Required: Vercel Blob Read+Write token
BLOB_READ_WRITE_TOKEN=changeme

# Strongly recommended: for /api/list-logs and /api/export-logs
ADMIN_TOKEN=changeme
