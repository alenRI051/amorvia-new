#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔎 Pre-commit: convert + lint + quick smoke"

npm run convert:v2 || exit 1
npm run lint:schemas || exit 1

# Run only the fast scenario_smoke.cy.js test
npm run test:smoke || exit 1
